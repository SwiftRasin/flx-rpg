trace("it works!");
trace("instance thing: " + instance.players);

var bool = true;

//var bg:FlxSprite;

create = function()
{
    var bg = new FlxSprite(0,0).loadGraphic(Assets.getFile("images/bg/grid_bg.png"));
    instance.add(bg);
    trace(Assets.mus("drums_test"));
    FlxG.sound.playMusic(Assets.mus("drums_test"));
}

updatePost = function()
{
    if (FlxG.keys.justPressed.I)
    {
        bool = !bool;
    }
    instance.players.forEach(function(plr) {
        //if (plr.settings.player == 0) {
            //plr.changeCostume("c64");
                //bool = !bool;
            if (bool)
            {
                plr.speed = plr.movementSettings["def"];
                plr.friction = plr.frictionSettings["def"];
            }
            else
            {
                plr.speed = plr.movementSettings["ice"];
                plr.friction = plr.frictionSettings["ice"];
            }   
        //}
        //else
        //    trace("couldn't change costume.");
    });
}


createPost = function()
{
    
    instance.players.forEach(function(plr) {
        if (plr.settings.player == 0) {
            //plr.changeCostume("c64");
        }
        else
            trace("couldn't change costume.");
    });
    
}


//trace("instance thing: " + instance.players[0]);